diff --git a/dist/workspace/sandbox/types.d.ts b/dist/workspace/sandbox/types.d.ts
index 587415c11dda0cb73e0b5324f4b397ebd402e9a7..6a66ffc5c3e78b6b009c78ae17984f804bc42e17 100644
--- a/dist/workspace/sandbox/types.d.ts
+++ b/dist/workspace/sandbox/types.d.ts
@@ -60,6 +60,8 @@ export interface CommandResult extends ExecutionResult {
  * Base type for both executeCommand and spawn.
  */
 export interface CommandOptions {
+    /** Raw process stdout/stderr encoding; defaults to UTF-8. LocalSandbox supports GBK and UTF-16LE. */
+    outputEncoding?: "utf-8" | "gbk" | "utf-16le";
     /** Timeout in milliseconds. Kills the process if exceeded. */
     timeout?: number;
     /** Environment variables */
diff --git a/dist/workspace/tools/execute-command.d.ts b/dist/workspace/tools/execute-command.d.ts
index f6a5eacac2098c75ec36ce21d2b62e94f07cabb4..9fea431ace45e2e8066da8cb822f9bcda9e6de69 100644
--- a/dist/workspace/tools/execute-command.d.ts
+++ b/dist/workspace/tools/execute-command.d.ts
@@ -4,6 +4,7 @@ import { z } from 'zod/v4';
  * Extended with `background` in tools.ts when sandbox.processes exists.
  */
 export declare const executeCommandInputSchema: z.ZodObject<{
+    outputEncoding: z.ZodOptional<z.ZodEnum<{ "utf-8": "utf-8"; gbk: "gbk"; "utf-16le": "utf-16le" }>>;
     command: z.ZodString;
     timeout: z.ZodOptional<z.ZodNullable<z.ZodPreprocess<z.ZodNumber>>>;
     cwd: z.ZodOptional<z.ZodNullable<z.ZodString>>;
@@ -11,6 +12,7 @@ export declare const executeCommandInputSchema: z.ZodObject<{
 }, z.core.$strip>;
 /** Schema with background param included. */
 export declare const executeCommandWithBackgroundSchema: z.ZodObject<{
+    outputEncoding: z.ZodOptional<z.ZodEnum<{ "utf-8": "utf-8"; gbk: "gbk"; "utf-16le": "utf-16le" }>>;
     command: z.ZodString;
     timeout: z.ZodOptional<z.ZodNullable<z.ZodPreprocess<z.ZodNumber>>>;
     cwd: z.ZodOptional<z.ZodNullable<z.ZodString>>;
@@ -19,6 +21,7 @@ export declare const executeCommandWithBackgroundSchema: z.ZodObject<{
 }, z.core.$strip>;
 /** Foreground-only tool (no background param in schema). */
 export declare const executeCommandTool: import("../../tools").Tool<{
+    outputEncoding?: "utf-8" | "gbk" | "utf-16le";
     command: string;
     timeout?: number | null | undefined;
     cwd?: string | null | undefined;
@@ -26,6 +29,7 @@ export declare const executeCommandTool: import("../../tools").Tool<{
 }, string, unknown, unknown, import("../../tools").ToolExecutionContext<unknown, unknown, unknown>, "mastra_workspace_execute_command", unknown>;
 /** Tool with background param in schema (used when sandbox.processes exists). */
 export declare const executeCommandWithBackgroundTool: import("../../tools").Tool<{
+    outputEncoding?: "utf-8" | "gbk" | "utf-16le";
     command: string;
     timeout?: number | null | undefined;
     cwd?: string | null | undefined;
diff --git a/dist/workspace/tools/grep.d.ts b/dist/workspace/tools/grep.d.ts
index c84b4f17d2a8d85a22ef7732146b29d9e84f9b44..1c2cafb61da6401e5fd0276d6abde2dfa2075e9a 100644
--- a/dist/workspace/tools/grep.d.ts
+++ b/dist/workspace/tools/grep.d.ts
@@ -1,4 +1,5 @@
 export declare const grepTool: import("../../tools").Tool<{
+    maxFileBytes?: number;
     pattern: string;
     path: string;
     contextLines: number;
diff --git a/dist/workspace-BmarCUep.cjs b/dist/workspace-BmarCUep.cjs
index becae5c55e5886b1fb60d3f9f76372d4618da1cf..b874efdbe8cb87d84ce4afd1b0610595ddac5122 100644
--- a/dist/workspace-BmarCUep.cjs
+++ b/dist/workspace-BmarCUep.cjs
@@ -2988,8 +2988,12 @@ var LocalProcessHandle = class extends ProcessHandle {
 			timedOut = true;
 			killProcessTree(this._numericPid, subprocess, "SIGTERM");
 		}, options.timeout) : void 0;
-		const stdoutDecoder = new string_decoder.StringDecoder();
-		const stderrDecoder = new string_decoder.StringDecoder();
+		const createDecoder = () => {
+			const decoder = new TextDecoder(options?.outputEncoding ?? "utf-8", { ignoreBOM: true });
+			return { write: (data) => decoder.decode(data, { stream: true }), end: () => decoder.decode() };
+		};
+		const stdoutDecoder = createDecoder();
+		const stderrDecoder = createDecoder();
 		let stdoutDecoderEnded = false;
 		let stderrDecoderEnded = false;
 		const flushStdoutDecoder = () => {
@@ -8462,6 +8466,7 @@ async function truncateOutput(output, tail, tokenLimit, tokenFrom) {
 * Extended with `background` in tools.ts when sandbox.processes exists.
 */
 const executeCommandInputSchema = zod_v4.z.object({
+	outputEncoding: zod_v4.z.enum(["utf-8", "gbk", "utf-16le"]).optional().describe("Encoding emitted by this command, applied to raw stdout/stderr for foreground and background processes. Default utf-8 (Node/Python configured for UTF-8). On Chinese Windows, cmd and Windows PowerShell commonly emit gbk (code page 936): select gbk after checking chcp. Do not mix different encodings in one command."),
 	command: zod_v4.z.string().describe("The shell command to execute (e.g., \"npm install\", \"ls -la src/\", \"cat file.txt | grep error\")"),
 	timeout: zod_v4.z.preprocess(coerceNumericString, zod_v4.z.number()).nullish().describe("Maximum execution time in seconds. Example: 60 for 1 minute."),
 	cwd: zod_v4.z.string().nullish().describe("Working directory for the command"),
@@ -8562,6 +8567,7 @@ async function executeCommand(input, context) {
 		const bgAbortSignal = bgConfig?.abortSignal === void 0 ? context?.abortSignal : bgConfig.abortSignal || void 0;
 		let handle;
 		handle = await sandbox.processes.spawn(command, {
+			outputEncoding: input.outputEncoding,
 			cwd: cwd ?? void 0,
 			timeout: timeout ?? void 0,
 			abortSignal: bgAbortSignal,
@@ -8600,6 +8606,7 @@ async function executeCommand(input, context) {
 	let stderr = "";
 	try {
 		const result = await sandbox.executeCommand(command, [], {
+			outputEncoding: input.outputEncoding,
 			timeout: timeout ?? void 0,
 			cwd: cwd ?? void 0,
 			abortSignal: context?.abortSignal,
@@ -8874,13 +8881,14 @@ Usage:
 	outputSchema: zod_v4.z.string(),
 	inputSchema: zod_v4.z.object({
 		pattern: zod_v4.z.string().describe("Regex pattern to search for"),
+		maxFileBytes: zod_v4.z.number().int().positive().optional().describe("Host-side per-file read budget in bytes, default 8388608 (8 MiB). Larger files are reported as skipped, not no-match. Raise explicitly for a known large text file; do not load large archives/media unnecessarily. Native streaming providers may not need this budget."),
 		path: zod_v4.z.string().optional().default(".").describe("File, directory, or glob pattern to search within (default: \".\"). A plain path searches that file or directory. A glob pattern (e.g., \"**/*.ts\", \"src/**/*.test.ts\") filters which files to search."),
 		contextLines: zod_v4.z.number().optional().default(0).describe("Number of lines of context to include before and after each match (default: 0)"),
 		maxCount: zod_v4.z.number().optional().describe("Maximum matches per file. Moves on to the next file after this many matches. Similar to grep -m flag."),
 		caseSensitive: zod_v4.z.boolean().optional().default(true).describe("Whether the search is case-sensitive (default: true)"),
 		includeHidden: zod_v4.z.boolean().optional().default(false).describe("Include hidden files and directories (names starting with \".\") in the search (default: false)")
 	}),
-	execute: async ({ pattern, path: inputPath = ".", contextLines = 0, maxCount, caseSensitive = true, includeHidden = false }, context) => {
+	execute: async ({ pattern, path: inputPath = ".", contextLines = 0, maxCount, caseSensitive = true, includeHidden = false, maxFileBytes = 8388608 }, context) => {
 		const { workspace, filesystem } = requireFilesystem(context);
 		await emitWorkspaceMetadata(context, WORKSPACE_TOOLS.FILESYSTEM.GREP);
 		const span = startWorkspaceSpan(context, workspace, {
@@ -8925,7 +8933,7 @@ Usage:
 			if (normalizedSearch === ".git" || normalizedSearch.endsWith("/.git")) filePaths = [];
 			else try {
 				const stat = await filesystem.stat(searchPath);
-				if (stat.type === "file") filePaths = require_workspace_skills.isTextFile(searchPath) ? [searchPath] : [];
+				if (stat.type === "file") filePaths = [searchPath];
 				else if (typeof filesystem.grep === "function") try {
 					nativeResults = await filesystem.grep({
 						pattern,
@@ -8982,7 +8990,6 @@ Usage:
 								if (ignoreFilter(checkPath)) continue;
 							}
 							if (entry.type === "file") {
-								if (!require_workspace_skills.isTextFile(entry.name)) continue;
 								if (globMatcher && !globMatcher(fullPath)) continue;
 								files.push(fullPath);
 							} else if (entry.type === "directory" && !entry.isSymlink) files.push(...collectFiles(fullPath));
@@ -8996,6 +9003,7 @@ Usage:
 			}
 			const outputLines = [];
 			const filesWithMatches = /* @__PURE__ */ new Set();
+			let skippedFiles = 0;
 			let totalMatchCount = 0;
 			let truncated = false;
 			let emittedContextHunk = false;
@@ -9047,7 +9055,6 @@ Usage:
 				const segments = rel.split("/");
 				if (segments.includes(".git")) continue;
 				if (!includeHidden && segments.some((segment) => segment.startsWith("."))) continue;
-				if (!require_workspace_skills.isTextFile(segments[segments.length - 1])) continue;
 				const fullPath = searchPath.endsWith("/") ? `${searchPath}${rel}` : `${searchPath}/${rel}`;
 				if (ignoreFilter) {
 					if (ignoreFilter(fullPath.replace(/^\.\//, ""))) continue;
@@ -9080,9 +9087,16 @@ Usage:
 				const batchPaths = filePaths.slice(batchStart, batchStart + GREP_FILESYSTEM_CONCURRENCY);
 				const batchContents = await require_workspace_skills.pMap(batchPaths, async (filePath) => {
 					try {
-						const raw = await filesystem.readFile(filePath, { encoding: "utf-8" });
-						return typeof raw === "string" ? raw : void 0;
+						if ((await filesystem.stat(filePath)).size > maxFileBytes) { skippedFiles++; return; }
+						const raw = await filesystem.readFile(filePath);
+						if (typeof raw === "string") {
+							if (raw.includes("\0")) { skippedFiles++; return; }
+							return raw;
+						}
+						if (raw.includes(0)) { skippedFiles++; return; }
+						return new TextDecoder("utf-8", { fatal: true }).decode(raw);
 					} catch {
+						skippedFiles++;
 						return;
 					}
 				}, { concurrency: GREP_FILESYSTEM_CONCURRENCY });
@@ -9117,6 +9131,7 @@ Usage:
 			const summaryParts = [`${totalMatchCount} match${totalMatchCount !== 1 ? "es" : ""}`];
 			summaryParts.push(`across ${filesWithMatches.size} file${filesWithMatches.size !== 1 ? "s" : ""}`);
 			if (truncated) summaryParts.push(`(truncated at ${GLOBAL_CAP})`);
+			if (skippedFiles) summaryParts.push(`(${skippedFiles} files skipped: binary, non-UTF-8, unreadable or above maxFileBytes=${maxFileBytes}; results are incomplete. Raise maxFileBytes for known large text files.)`);
 			const summary = summaryParts.join(" ");
 			outputLines.unshift(summary, "---");
 			const output = await applyTokenLimit(outputLines.join("\n"), workspace.getToolsConfig()?.[WORKSPACE_TOOLS.FILESYSTEM.GREP]?.maxOutputTokens, "end");
diff --git a/dist/workspace-_WoSs-8p.js b/dist/workspace-_WoSs-8p.js
index d371176e82c220be9fcf2d7c35ce61202b6b71dd..3a5cb2792903128d4acdeed364c91a0b047490d5 100644
--- a/dist/workspace-_WoSs-8p.js
+++ b/dist/workspace-_WoSs-8p.js
@@ -2983,8 +2983,12 @@ var LocalProcessHandle = class extends ProcessHandle {
 			timedOut = true;
 			killProcessTree(this._numericPid, subprocess, "SIGTERM");
 		}, options.timeout) : void 0;
-		const stdoutDecoder = new StringDecoder();
-		const stderrDecoder = new StringDecoder();
+		const createDecoder = () => {
+			const decoder = new TextDecoder(options?.outputEncoding ?? "utf-8", { ignoreBOM: true });
+			return { write: (data) => decoder.decode(data, { stream: true }), end: () => decoder.decode() };
+		};
+		const stdoutDecoder = createDecoder();
+		const stderrDecoder = createDecoder();
 		let stdoutDecoderEnded = false;
 		let stderrDecoderEnded = false;
 		const flushStdoutDecoder = () => {
@@ -8457,6 +8461,7 @@ async function truncateOutput(output, tail, tokenLimit, tokenFrom) {
 * Extended with `background` in tools.ts when sandbox.processes exists.
 */
 const executeCommandInputSchema = z.object({
+	outputEncoding: z.enum(["utf-8", "gbk", "utf-16le"]).optional().describe("Encoding emitted by this command, applied to raw stdout/stderr for foreground and background processes. Default utf-8 (Node/Python configured for UTF-8). On Chinese Windows, cmd and Windows PowerShell commonly emit gbk (code page 936): select gbk after checking chcp. Do not mix different encodings in one command."),
 	command: z.string().describe("The shell command to execute (e.g., \"npm install\", \"ls -la src/\", \"cat file.txt | grep error\")"),
 	timeout: z.preprocess(coerceNumericString, z.number()).nullish().describe("Maximum execution time in seconds. Example: 60 for 1 minute."),
 	cwd: z.string().nullish().describe("Working directory for the command"),
@@ -8557,6 +8562,7 @@ async function executeCommand(input, context) {
 		const bgAbortSignal = bgConfig?.abortSignal === void 0 ? context?.abortSignal : bgConfig.abortSignal || void 0;
 		let handle;
 		handle = await sandbox.processes.spawn(command, {
+			outputEncoding: input.outputEncoding,
 			cwd: cwd ?? void 0,
 			timeout: timeout ?? void 0,
 			abortSignal: bgAbortSignal,
@@ -8595,6 +8601,7 @@ async function executeCommand(input, context) {
 	let stderr = "";
 	try {
 		const result = await sandbox.executeCommand(command, [], {
+			outputEncoding: input.outputEncoding,
 			timeout: timeout ?? void 0,
 			cwd: cwd ?? void 0,
 			abortSignal: context?.abortSignal,
@@ -8869,13 +8876,14 @@ Usage:
 	outputSchema: z.string(),
 	inputSchema: z.object({
 		pattern: z.string().describe("Regex pattern to search for"),
+		maxFileBytes: z.number().int().positive().optional().describe("Host-side per-file read budget in bytes, default 8388608 (8 MiB). Larger files are reported as skipped, not no-match. Raise explicitly for a known large text file; do not load large archives/media unnecessarily. Native streaming providers may not need this budget."),
 		path: z.string().optional().default(".").describe("File, directory, or glob pattern to search within (default: \".\"). A plain path searches that file or directory. A glob pattern (e.g., \"**/*.ts\", \"src/**/*.test.ts\") filters which files to search."),
 		contextLines: z.number().optional().default(0).describe("Number of lines of context to include before and after each match (default: 0)"),
 		maxCount: z.number().optional().describe("Maximum matches per file. Moves on to the next file after this many matches. Similar to grep -m flag."),
 		caseSensitive: z.boolean().optional().default(true).describe("Whether the search is case-sensitive (default: true)"),
 		includeHidden: z.boolean().optional().default(false).describe("Include hidden files and directories (names starting with \".\") in the search (default: false)")
 	}),
-	execute: async ({ pattern, path: inputPath = ".", contextLines = 0, maxCount, caseSensitive = true, includeHidden = false }, context) => {
+	execute: async ({ pattern, path: inputPath = ".", contextLines = 0, maxCount, caseSensitive = true, includeHidden = false, maxFileBytes = 8388608 }, context) => {
 		const { workspace, filesystem } = requireFilesystem(context);
 		await emitWorkspaceMetadata(context, WORKSPACE_TOOLS.FILESYSTEM.GREP);
 		const span = startWorkspaceSpan(context, workspace, {
@@ -8920,7 +8928,7 @@ Usage:
 			if (normalizedSearch === ".git" || normalizedSearch.endsWith("/.git")) filePaths = [];
 			else try {
 				const stat = await filesystem.stat(searchPath);
-				if (stat.type === "file") filePaths = isTextFile(searchPath) ? [searchPath] : [];
+				if (stat.type === "file") filePaths = [searchPath];
 				else if (typeof filesystem.grep === "function") try {
 					nativeResults = await filesystem.grep({
 						pattern,
@@ -8977,7 +8985,6 @@ Usage:
 								if (ignoreFilter(checkPath)) continue;
 							}
 							if (entry.type === "file") {
-								if (!isTextFile(entry.name)) continue;
 								if (globMatcher && !globMatcher(fullPath)) continue;
 								files.push(fullPath);
 							} else if (entry.type === "directory" && !entry.isSymlink) files.push(...collectFiles(fullPath));
@@ -8991,6 +8998,7 @@ Usage:
 			}
 			const outputLines = [];
 			const filesWithMatches = /* @__PURE__ */ new Set();
+			let skippedFiles = 0;
 			let totalMatchCount = 0;
 			let truncated = false;
 			let emittedContextHunk = false;
@@ -9042,7 +9050,6 @@ Usage:
 				const segments = rel.split("/");
 				if (segments.includes(".git")) continue;
 				if (!includeHidden && segments.some((segment) => segment.startsWith("."))) continue;
-				if (!isTextFile(segments[segments.length - 1])) continue;
 				const fullPath = searchPath.endsWith("/") ? `${searchPath}${rel}` : `${searchPath}/${rel}`;
 				if (ignoreFilter) {
 					if (ignoreFilter(fullPath.replace(/^\.\//, ""))) continue;
@@ -9075,9 +9082,16 @@ Usage:
 				const batchPaths = filePaths.slice(batchStart, batchStart + GREP_FILESYSTEM_CONCURRENCY);
 				const batchContents = await pMap(batchPaths, async (filePath) => {
 					try {
-						const raw = await filesystem.readFile(filePath, { encoding: "utf-8" });
-						return typeof raw === "string" ? raw : void 0;
+						if ((await filesystem.stat(filePath)).size > maxFileBytes) { skippedFiles++; return; }
+						const raw = await filesystem.readFile(filePath);
+						if (typeof raw === "string") {
+							if (raw.includes("\0")) { skippedFiles++; return; }
+							return raw;
+						}
+						if (raw.includes(0)) { skippedFiles++; return; }
+						return new TextDecoder("utf-8", { fatal: true }).decode(raw);
 					} catch {
+						skippedFiles++;
 						return;
 					}
 				}, { concurrency: GREP_FILESYSTEM_CONCURRENCY });
@@ -9112,6 +9126,7 @@ Usage:
 			const summaryParts = [`${totalMatchCount} match${totalMatchCount !== 1 ? "es" : ""}`];
 			summaryParts.push(`across ${filesWithMatches.size} file${filesWithMatches.size !== 1 ? "s" : ""}`);
 			if (truncated) summaryParts.push(`(truncated at ${GLOBAL_CAP})`);
+			if (skippedFiles) summaryParts.push(`(${skippedFiles} files skipped: binary, non-UTF-8, unreadable or above maxFileBytes=${maxFileBytes}; results are incomplete. Raise maxFileBytes for known large text files.)`);
 			const summary = summaryParts.join(" ");
 			outputLines.unshift(summary, "---");
 			const output = await applyTokenLimit(outputLines.join("\n"), workspace.getToolsConfig()?.[WORKSPACE_TOOLS.FILESYSTEM.GREP]?.maxOutputTokens, "end");
